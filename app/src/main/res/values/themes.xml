<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base App Theme -->
    <style name="Theme.DrawRun" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Will be overridden by Compose theme -->
    </style>
    
    <!-- Splash Screen Theme (Android 12+) -->
    <style name="Theme.DrawRun.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_light</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background_light</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.DrawRun</item>
    </style>
</resources>
