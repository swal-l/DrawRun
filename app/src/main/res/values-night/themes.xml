<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme (Dark Mode) -->
    <style name="Theme.DrawRun.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_background_dark</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.DrawRun</item>
    </style>
</resources>
